'use strict';

describe('the game controller', function() {

  // var modalInstance = {
  //   result:{
  //     then:function(doFun) {
  //     }
  //   }
  // };

  var gameEngine = {
    
  };

  var modal = {
    open:function(props){}
  };

  var ticTacToeGameController, $scope;

  beforeEach(inject(function($rootScope, $controller, $modal) {
    $scope = $rootScope.$new();
    ticTacToeGameController = function() {
      return $controller('TicTacToeGameController', {'$scope':$scope, '$modal':$modal, 'GameEngine':gameEngine});
    };
    $scope.vm = ticTacToeGameController;
  }));


  it('should update the gameboard after a play', function() {

  });

  it('should update scope when game is over', function() {

  });


});
